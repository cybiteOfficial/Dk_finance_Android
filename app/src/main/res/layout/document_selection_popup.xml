<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="330dp"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Selected section -->
    <TextView
        android:id="@+id/selected_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Selected"
        android:textColor="@color/primary"
        android:textSize="20sp"
        android:textStyle="bold"
        android:paddingBottom="8dp" />

    <LinearLayout
        android:id="@+id/selected_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">
        <!-- Selected items will be dynamically added here -->
    </LinearLayout>

    <!-- Not Selected section -->
    <TextView
        android:id="@+id/not_selected_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Not Selected"
        android:textColor="@color/primary"
        android:textSize="20sp"
        android:textStyle="bold"
        android:paddingTop="16dp"
        android:paddingBottom="8dp" />

    <LinearLayout
        android:id="@+id/not_selected_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">
        <!-- Default: Add all add_more_ layouts to not selected section -->
        <include layout="@layout/add_more_pan"/>
        <include layout="@layout/add_more_passport"/>
        <include layout="@layout/add_more_driving"/>
        <!-- Add more add_more_ layouts here if needed -->
    </LinearLayout>

    <Button
        android:id="@+id/save_changes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:backgroundTint="@color/primary"
        android:text="SAVE CHANGES"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:paddingVertical="12dp"
        android:gravity="center_horizontal"
        android:layout_gravity="center_horizontal"
        app:cornerRadius="8dp"/>

</LinearLayout>
